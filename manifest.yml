files:
  - source: 99-noto-mono-color-emoji.conf
    destination: fontconfig/conf.d/99-noto-mono-color-emoji.conf

  - source: alacritty.yml
    destination: .config/alacritty/alacritty.yml
    location: Home

  - source: config.fish
    destination: fish/config.fish

  - source: gtk.css
    destination: gtk-3.0/gtk.css

  - source: neovim
    destination: .config/nvim
    location: Home

  - source: starship.toml
    destination: starship.toml

  - source: wezterm.lua
    destination: wezterm/wezterm.lua

  - source: ipython_config.py
    destination: .ipython/profile_default/ipython_config.py
    location: Home

  - source: gitconfig
    destination: .gitconfig
    location: Home

  - source: gitconfig.graphite
    destination: .gitconfig.graphite
    location: Home

  - source: gitconfig.personal
    destination: .gitconfig.personal
    location: Home

  - source: gitignore
    destination: .gitignore
    location: Home

  - source: ideavimrc
    destination: .ideavimrc
    location: Home

  - source: inputrc
    destination: .inputrc
    location: Home

  - source: tmux.conf
    destination: .tmux.conf
    location: Home

  - source: zshrc
    destination: .zshrc
    location: Home

  - source: aliases
    destination: .zsh/aliases
    location: Home

  - source: zsh_custom
    destination: .zsh_custom
    location: Home

  - source: bin/git-diff-branch.sh
    destination: bin/git-diff-branch
    location: Home

  - source: psqlrc
    destination: .psqlrc
    location: Home

  - source: starship.toml
    destination: .config/starship.toml
    location: Home

  - source: hammerspoon
    destination: .hammerspoon
    location: Home

  - source: bin/brew_packages.py
    destination: bin/brew-packages
    location: Home

  - source: bin/timestamp.sh
    destination: bin/timestamp
    location: Home

  - source: bin/git-clear.sh
    destination: bin/git-clear
    location: Home

  - source: bin/pretty.sh
    destination: bin/pretty
    location: Home

  - source: aliases.toml
    destination: .aliases.toml
    location: Home
